import { OutputFilterPipe } from './output-filter.pipe';

describe('OutputFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OutputFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
